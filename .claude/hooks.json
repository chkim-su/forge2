{
  "$comment": "Project-level hooks INTENTIONALLY EMPTY - All workflow hooks are in hooks/hooks.json to avoid global contamination. Hooks here would run on ALL prompts in the project, not just /assist commands."
}
